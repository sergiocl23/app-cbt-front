import { Routes } from '@angular/router';
import { MeetingsLayoutPageComponent } from './pages/meetings-layout-page/meetings-layout-page.component';

export const routes: Routes = [
  {
    path: '',
    component: MeetingsLayoutPageComponent
  }
];
